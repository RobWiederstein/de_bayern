# R
.Rproj.user
.Rhistory
.RData
.Ruserdata

# htmlwidgets selfcontained output keeps everything inline; no _files dir.
# Keep index.html tracked (GitHub Pages serves it).

# Working notes — kept local, not published to the public mirror
claude.md
TODO.md

# Source data is fetched/local, never tracked. Regenerate with R/fetch_data.R.
data/
